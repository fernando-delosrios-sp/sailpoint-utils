## Purpose

Legacy target client layer (`src/my-client.ts`) removed in favor of SDK loopback and custom-operation framework. Configuration validation and connectivity are handled per invocation via the standard input envelope.

## Requirements
