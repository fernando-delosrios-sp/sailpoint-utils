## 1. Contract tests

- [x] 1.1 Add named tests in `tests/test_ingest_docs.py` proving service-account creation remains separate, an existing account stops before grants, and the grant action references one generated artifact.
- [x] 1.2 Add named tests proving the generated IAM artifact contains the pinned archive's complete custom-role permissions and exactly 12 predefined `roles/*` bindings.
- [x] 1.3 Add named tests for organization-project, host-project, and billing-dependent API defaults, including the absence of a new Connect-time API-selection fork.
- [x] 1.4 Add named tests proving organization audit logging is an Operator-only console step with non-secret evidence.
- [x] 1.5 Add named tests proving stale generated bytes or checksums fail validation and both Skill trees, Row catalogs, and Typed action references remain aligned.
- [x] 1.6 Add a named test proving rollback removes only run-owned IAM changes and a run-created service account while leaving enabled APIs unchanged.

## 2. Terraform-derived artifact generation

- [x] 2.1 Extend `integration_catalog.py` to read the pinned `Entro GCP Terraform onboarding.zip` deterministically and extract the custom organization role permissions and 12 predefined bindings without changing the source archive.
- [x] 2.2 Generate one executable IAM grant artifact with deterministic bytes, verification, and run-scoped rollback behavior.
- [x] 2.3 Extract the archive's organization-project, host-project, and billing-dependent API defaults and generate the corresponding deterministic API enablement artifact.
- [x] 2.4 Compute and expose SHA-256 pins for both generated artifacts, failing closed when expected Terraform structures, binding counts, or API groups are missing.

## 3. GCP Configuration plan

- [x] 3.1 Update `catalog_contracts.py` to keep the collision-aware `gcloud iam service-accounts create` mutation as a separate Typed action.
- [x] 3.2 Add the generated IAM grant Typed action after service-account creation with exact target, expected change, verification, source, checksum, and scoped rollback metadata.
- [x] 3.3 Replace the partial API action with the generated Terraform-default action, including its billing condition and an irreversible-impact note that rollback leaves APIs enabled.
- [x] 3.4 Add the organization audit-log Prep step as Operator-only with Google Cloud console instructions, reason, and non-secret evidence.

## 4. Publication and drift validation

- [x] 4.1 Extend catalog validation to compare the pinned Terraform source, generated bytes, checksums, Typed action references, and Row catalog pins.
- [x] 4.2 Regenerate `documentation/integrations.json` and both `entro-connect/integrations/google-cloud-platform/` Row catalog folders, committing generator edits and generated outputs together.
- [x] 4.3 Verify the two Skill trees contain byte-identical IAM and API artifacts and that hand edits or checksum drift are rejected.

## 5. Verification

- [x] 5.1 Confirm canonical test command for this environment: `uv run python -m pytest`
- [x] 5.2 Run the focused GCP ingest tests and confirm every delta-spec scenario is covered by a named automated test.
- [x] 5.3 Run `uv run python -m pytest` and confirm the complete suite passes without vendor credentials or network access.
- [x] 5.4 Run `openspec validate complete-gcp-connect-actions --type change --json` and resolve every validation error.

## 6. Documentation

- [x] 6.1 Review the regenerated GCP entries in `documentation/integrations.json` for exact operator-visible action, evidence, verification, and rollback text.
- [x] 6.2 Review both generated `entro-connect/integrations/google-cloud-platform/catalog.json` files and their adjacent IAM/API artifacts for self-contained Connect instructions.

## 7. Changelog

- [x] 7.1 Invoke `changelog-generator` during apply to add the GCP Console-manual action-completeness entry to `CHANGELOG.md`.
- [x] 7.2 Confirm the changelog covers exact IAM grants, Terraform-default API enablement, operator-only audit logging, and generated-artifact drift protection.
